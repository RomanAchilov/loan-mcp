# Инструкция по деплою на Vercel

## 📋 Шаг 1: Импорт проекта в Vercel

1. Перейдите на **https://vercel.com**
2. Авторизуйтесь через GitHub
3. Нажмите **"Add New..." → "Project"**
4. Выберите репозиторий **`loan-mcp`**
5. Нажмите **"Import"**

## ⚙️ Шаг 2: Настройки проекта

Vercel автоматически определит настройки из `vercel.json`:

- ✅ **Framework Preset**: Vite
- ✅ **Build Command**: `npm run build`
- ✅ **Output Directory**: `.output/public`
- ✅ **Install Command**: `npm install`

**Ничего менять не нужно!** Просто нажмите **"Deploy"**

## 🌐 Шаг 3: Получение URL

После успешного деплоя вы получите URL вида:
```
https://loan-mcp.vercel.app
```

или

```
https://loan-mcp-[your-username].vercel.app
```

## 📦 Шаг 4: Подключение виджета в ChatGPT

После деплоя виджет будет доступен по адресу:

```html
<link rel="stylesheet" href="https://YOUR-APP.vercel.app/chatgpt-widget.css">
<script src="https://YOUR-APP.vercel.app/chatgpt-widget.js"></script>
```

**Замените `YOUR-APP.vercel.app` на ваш реальный URL!**

## 🔧 Настройки CORS (уже настроены)

В `vercel.json` уже настроены правильные CORS заголовки для виджета:

- ✅ `Access-Control-Allow-Origin: *` - виджет доступен из любого источника
- ✅ `Cache-Control` - оптимизация кеширования
- ✅ Security headers - защита приложения

## 🚀 Автоматический деплой

Теперь при каждом `git push origin main`:
- ✅ Vercel автоматически задеплоит изменения
- ✅ Виджет обновится автоматически
- ✅ URL останется тем же

## 🔗 MCP Endpoint

После деплоя MCP сервер будет доступен по адресу:

```
https://YOUR-APP.vercel.app/mcp
```

### Тестирование MCP:

```bash
# Получить информацию о сервере
curl https://YOUR-APP.vercel.app/mcp

# Получить все кредиты
curl -X POST https://YOUR-APP.vercel.app/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
      "name": "getLoans"
    },
    "id": 1
  }'

# Показать конкретный кредит
curl -X POST https://YOUR-APP.vercel.app/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
      "name": "showLoan",
      "arguments": {
        "id": 1
      }
    },
    "id": 1
  }'
```

## 🎯 Настройка пользовательского домена (опционально)

1. Перейдите в **Settings → Domains**
2. Нажмите **"Add"**
3. Введите ваш домен (например, `loan.mydomain.com`)
4. Следуйте инструкциям по настройке DNS

## ⚠️ Важно!

После первого деплоя:
1. Скопируйте URL вашего приложения
2. Обновите его в вашей MCP конфигурации для ChatGPT
3. Виджет должен работать из любого источника благодаря CORS

## 📝 Переменные окружения (опционально)

Если хотите переопределить автоматический URL:

1. Перейдите в **Settings → Environment Variables**
2. Добавьте переменную:
   - **Name**: `VITE_APP_URL`
   - **Value**: `https://your-custom-domain.com/`
3. Redeploy проект

## 🐛 Troubleshooting

### Виджет не загружается
- Проверьте URL в console браузера
- Убедитесь что файлы доступны: `https://YOUR-APP.vercel.app/chatgpt-widget.js`
- Проверьте CORS ошибки в консоли

### MCP не работает
- Проверьте что endpoint доступен: `https://YOUR-APP.vercel.app/mcp`
- Проверьте логи в Vercel Dashboard

### 500 ошибка при билде
- Проверьте логи билда в Vercel
- Убедитесь что все зависимости установлены
- Попробуйте локальный билд: `npm run build`

