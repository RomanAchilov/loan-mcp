# MCP Сервер для Кредитов Онлайн

Этот проект представляет собой MCP (Model Context Protocol) сервер для ChatGPT, предоставляющий информацию о кредитных продуктах.

## Возможности

- 🏦 **7 кредитных продуктов**: Экспресс-кредит, Потребительский кредит, Кредит под залог, Рефинансирование, Кредит для бизнеса, Автокредит, Ипотека
- 💳 **Детальная информация**: Сумма, процентная ставка, срок, ежемесячный платеж, скорость одобрения
- 🤖 **ChatGPT интеграция**: Встроенный виджет для отображения кредитных продуктов в ChatGPT
- 🎨 **Современный UI**: Красивый интерфейс на базе TailwindCSS
- 🚀 **TanStack Start**: Полноценный full-stack фреймворк с SSR

## Технологии

- **TanStack Start** - Full-stack React фреймворк
- **TanStack Router** - Типобезопасный роутинг
- **Model Context Protocol SDK** - Интеграция с ChatGPT
- **TailwindCSS** - Стилизация
- **TypeScript** - Типизация
- **Vite** - Сборка
- **Nitro** - Server runtime

## Запуск проекта

### Разработка

```bash
npm install
npm run dev
```

Сервер будет доступен на http://localhost:3000

### Продакшн

```bash
npm run build
npm start
```

## MCP Endpoints

### GET /mcp
Получить информацию о MCP сервере

### POST /mcp
Выполнить MCP запрос

## MCP Tools

### getLoans
Получить список всех доступных кредитных продуктов

```json
{
  "method": "tools/call",
  "params": {
    "name": "getLoans"
  }
}
```

### showLoan
Показать детальную информацию о конкретном кредитном продукте

```json
{
  "method": "tools/call",
  "params": {
    "name": "showLoan",
    "arguments": {
      "id": 1
    }
  }
}
```

## Структура данных кредита

```typescript
interface LoanProduct {
  id: number;
  name: string;
  image: string;
  description: string;
  shortDescription: string;
  amount: number;           // Сумма кредита
  interestRate: number;     // Процентная ставка
  termMonths: number;       // Срок в месяцах
  monthlyPayment: number;   // Ежемесячный платеж
  approvalSpeed: string;    // Скорость одобрения
}
```

## Маршруты

- `/` - Главная страница
- `/example/loans` - Список кредитных продуктов
- `/example/loans/:loanId` - Детальная страница кредитного продукта
- `/mcp` - MCP endpoint

## ChatGPT Виджет

Проект включает в себя виджет для ChatGPT, который отображает информацию о кредитном продукте прямо в интерфейсе ChatGPT.

### Сборка виджета

```bash
npm run build:widget
```

Виджет будет собран в папку `public/`

## Разработка

### Добавление нового кредитного продукта

1. Откройте `src/data/example-loans.ts`
2. Добавьте новый объект в массив `loanProducts`
3. Добавьте изображение в папку `public/`

### Настройка базового URL

Откройте `src/lib/config.ts` и настройте `baseURL` для вашего окружения.

## License

MIT
